extends layout

block content
  h1.mb-3 Bienvenido, #{user.nombre} #{user.apellido}
  p.text-muted Rol: #{user.rol}
  p.text-muted Email: #{user.email}
  hr

  p.mt-4 Bienvenido al sistema HIS de internación. Seleccioná una opción del menú lateral para comenzar.