extends ../layout

block content
  .text-center.mt-5
    h1.display-4 404
    h3 Página no encontrada
    p.text-muted La ruta que intentaste acceder no existe.
    a.btn.btn-primary.mt-3(href="/") Volver al inicio